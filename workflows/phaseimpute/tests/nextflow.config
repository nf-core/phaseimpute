process {
    withName: SHAPEIT5_PHASECOMMON {
        cpus = 1
        ext.args = "--seed 1"
        ext.suffix = "bcf"
        ext.prefix = { "${meta.id}_${meta.chunk.replace(':',"_")}_chunks" }
    }
}
