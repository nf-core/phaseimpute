params {
    max_memory = '7.GB'
}

process {
    withName: BCFTOOLS_ANNOTATE {
        ext.args = [
            "-Oz",
            "--no-version",
            "--write-index=tbi"
        ].join(' ')
        ext.prefix = { "${meta.id ? meta.id : meta.panel_id}_chrrename" }
    }
}
