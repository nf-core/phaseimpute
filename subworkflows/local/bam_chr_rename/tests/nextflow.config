params {
    max_memory = '2.GB'
}

process {
    withName: SAMTOOLS_REHEADER {
        ext.args = [
            "--no-version"
        ].join(' ')
        ext.prefix = { "${meta.id}_chrrename" }
    }
}
