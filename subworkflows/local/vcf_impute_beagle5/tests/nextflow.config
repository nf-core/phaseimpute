process {
    withName: BEAGLE5_BEAGLE {
        ext.prefix = { "${meta.id}_imputed" }
        // Arguments for imputation with reference panel
        ext.args = ""
    }

    withName: BCFTOOLS_INDEX_IMPUTE {
        ext.args = "--csi"
    }
}
